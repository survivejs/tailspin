import { assertEquals } from "../deps.ts";
import parseCode from "./parse-code.ts";

Deno.test("parses simple source", () => {
  /*const componentSource = "<div>demo</div>";

  assertEquals(
    parseCode(
      { name: "Demo", source: `const Demo = () => ${componentSource};` },
    ),
    componentSource,
  );*/
});
